this program is an IM server which written by C++.

the version now is pre-alpha used raw socket api and multiThread of windows.



I am writting the alpha version for Linux instead windows.
In the new version , the raw socket api will be instead of Muduo network library , and I will use memecached and mySql,
and epoll will instead multiThread.

the code of new version is testing now, will be pushed a coulpe week later. 



